<div class="navbar navbar-full navbar-dark nav-bg"  ng-controller="NavBarController">
  <div class="grit-icon">
    <span style="color: white;">
      Gritize
    </span>
    <%= image_tag 'grit-icon.png', size:"75x75"%>

  </div>

  <ul class="nav navbar-nav " style="color: white;">
    <li ng-hide="signedIn()"><a a class="c-links"  href="#/home">Home</a></li>
    <li ng-hide="signedIn()"><a class="c-links"  href="#/login">Log In</a></li>
    <li ng-hide="signedIn()"><a class="c-links" href="" ui-sref="register">Register</a></li>
    <li ng-show="signedIn()"><a class="c-links" href="" ui-sref="profile.routines({user_id:user.id})">Routines</a></li>
    <li ng-show="signedIn()"><a class="pull-right c-links" href="#" ui-sref="profile({user_id:user.id})">Profile</a></li>
    <li ng-show="signedIn()"><a class="c-links" ng-click="logout()">Log Out</a></li>

  </ul>

    <a id="profile-name" ng-show="signedIn()" href="#" class="c-links" ui-sref="profile({user_id:user.id})">Logged in as-({{ user.username }})</a>

</div>
